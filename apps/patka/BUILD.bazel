load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "patka",
    srcs = glob(["src/**/*.rs"]),
    edition = "2021",
    visibility = ["//visibility:public"],
    deps = [
        "@crates//:log",
        "@crates//:ratatui",
    ],
)
